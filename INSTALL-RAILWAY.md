# دليل تنصيب البوت على Railway

دليل كامل من الصفر. اتبع المراحل بالترتيب.

---

# المرحلة 1 — تجهيز الحسابات

قبل لمس Railway، جهّز هذه الأشياء. ستحتاجها كلها.

## 1.1 إنشاء البوتات على تيليجرام

افتح **@BotFather** على تيليجرام وأنشئ **ثلاثة بوتات** (الثاني والثالث اختياريان):

| البوت | الأمر | ما ستحصل عليه |
|---|---|---|
| البوت الرئيسي (المتجر) | `/newbot` | `BOT_TOKEN` |
| بوت الدعم | `/newbot` | `SUPPORT_BOT_TOKEN` + اسم المستخدم |
| بوت ChatGPT Business | `/newbot` | `CHATGPT_BOT_TOKEN` + اسم المستخدم |

التوكن يبدو هكذا: `7123456789:AAH-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

> ⚠️ التوكن كلمة سر. من يملكه يتحكم بالبوت بالكامل.

## 1.2 معرفة رقمك في تيليجرام

أرسل أي رسالة إلى **@userinfobot** وسيرد برقمك، مثل `5626665035`.

## 1.3 معرّفات القناة والمجموعة

إن أردت إلزام الزبائن بالانضمام لقناة أو مجموعة:

1. أنشئ القناة/المجموعة
2. **أضف البوت الرئيسي كمشرف (Admin)** فيها — إلزامي، وإلا لن يستطيع التحقق
3. أضف **@userinfobot** إليها مؤقتًا، سيعطيك المعرّف
4. أخرجه بعدها

المعرّف يبدأ بـ `-100` مثل: `-1001234567890`

الرابط يكون: `https://t.me/اسم_القناة` أو رابط دعوة إن كانت خاصة.

## 1.4 مفاتيح Binance (لإيداعات USDT)

من حسابك على Binance → **API Management** → **Create API**:

```
✅ Enable Reading          ← فعّل هذا فقط
❌ Enable Spot Trading     ← اتركه مطفأً
❌ Enable Withdrawals      ← اتركه مطفأً قطعًا
```

> 🔴 **لا تفعّل صلاحية السحب إطلاقًا.** البوت يحتاج القراءة فقط ليتحقق من الإيداعات. لو تسرّب المفتاح مع صلاحية السحب، يُفرَّغ حسابك.

احفظ `API Key` و `Secret Key`. الـ Secret يُعرض **مرة واحدة فقط**.

## 1.5 عناوين استقبال USDT

من Binance → **Deposit** → اختر USDT:
- شبكة **TRC20** → انسخ العنوان (يبدأ بـ `T`)
- شبكة **BEP20** → انسخ العنوان (يبدأ بـ `0x`)

## 1.6 CryptoBot (اختياري، لكن موصى به بشدة)

افتح **@CryptoBot** → `/start` → **Crypto Pay** → **Create App** → احفظ التوكن.

> 💡 CryptoBot أسهل طريقة شحن للزبون: يضغط رابطًا ويدفع، والمحفظة تُشحن تلقائيًا بلا TXID ولا خطوات.

---

# المرحلة 2 — رفع الكود على GitHub

1. أنشئ حسابًا على [github.com](https://github.com)
2. **New repository** → سمّه `digitrust-bot` → اجعله **Private** ✅
3. **Add file** → **Upload files**
4. فُكّ ضغط ملف البوت، **افتح** المجلد بداخله، وحدّد **محتوياته** (لا المجلد نفسه)
5. اسحبها إلى الصفحة → **Commit changes**

> ⚠️ الخطأ الشائع: سحب المجلد نفسه. عندها تصير المسارات `digitrust-bot-main/index.js` بدل `index.js` وينكسر كل شيء.

بعد الرفع يجب أن ترى `index.js` و `package.json` في **جذر** المستودع مباشرة.

---

# المرحلة 3 — إنشاء المشروع على Railway

1. سجّل في [railway.app](https://railway.app) بحساب GitHub
2. **New Project** → **Deploy from GitHub repo**
3. اختر المستودع → Railway سيبدأ البناء تلقائيًا

**سيفشل أول Deploy — هذا طبيعي ومتوقع**، لأن المتغيرات لم تُضبط بعد.

Railway يكتشف Node.js تلقائيًا ويشغّل `npm start`. لا تحتاج ضبط أي شيء هنا.

---

# المرحلة 4 — الـ Volume (أهم خطوة على الإطلاق)

> 🔴 **لا تتخطَّ هذه المرحلة.** بدون Volume، قاعدة البيانات تُمسح مع **كل** deploy: كل الزبائن، الأرصدة، الطلبات، المخزون — كل شيء يضيع.

## الخطوات

1. في مشروعك، اضغط على **الخدمة (Service)** لفتحها
2. اذهب إلى تبويب **Settings**
3. انزل إلى قسم **Volumes**
4. اضغط **+ New Volume** (أو **Add Volume**)
5. املأ:

| الحقل | القيمة |
|---|---|
| **Name** | `digitrust-bot-volume` |
| **Mount path** | `/app/data` |

6. **Add** / **Create**

## ثم — وهذا مكمّل إلزامي

أضف المتغير:

```
DB_PATH = /app/data/store.db
```

> `/app/data` هو مكان تثبيت الـ Volume، و `store.db` اسم ملف قاعدة البيانات بداخله. **يجب أن يبدأ المسار بنفس الـ Mount path**، وإلا فالملف خارج الـ Volume ويُمسح.

## كيف تتأكد أنه يعمل

بعد أول تشغيل ناجح، أنشئ منتجًا تجريبيًا، ثم اضغط **Redeploy**. إن بقي المنتج موجودًا بعد إعادة النشر ← الـ Volume يعمل. إن اختفى ← المسار خاطئ.

---

# المرحلة 5 — المتغيرات

في الخدمة → تبويب **Variables** → **+ New Variable** لكل واحد.

## 5.1 إلزامية — البوت لا يعمل بدونها

| المتغير | القيمة | مثال |
|---|---|---|
| `BOT_TOKEN` | توكن البوت الرئيسي من BotFather | `7123456789:AAH-xxxx...` |
| `ADMIN_IDS` | أرقام المشرفين، **مفصولة بفاصلة بلا مسافات** | `5626665035` أو `5626665035,987654321` |
| `ADMIN_ID` | رقمك أنت (مفرد) | `5626665035` |
| `DB_PATH` | مسار قاعدة البيانات على الـ Volume | `/app/data/store.db` |

> **لماذا متغيران للأدمن؟** `ADMIN_IDS` (جمع) للوحة `/admin` في البوت الرئيسي، و`ADMIN_ID` (مفرد) لبوت الدعم وبوت ChatGPT وإشعارات الاسترجاع. ضع رقمك في كليهما.

إن نقص `BOT_TOKEN` سيتوقف البوت فورًا برسالة في اللوغ.

## 5.2 المتجر والدعم

| المتغير | القيمة | مثال |
|---|---|---|
| `STORE_NAME` | اسم متجرك كما يظهر للزبائن | `MY DIGITAL STORE` |
| `SUPPORT_USERNAME` | يوزر الدعم بلا `@` | `MyStoreSupport` |
| `SUPPORT_BOT_TOKEN` | توكن بوت الدعم | `7987654321:AAG-yyyy...` |
| `SUPPORT_BOT_USERNAME` | يوزر بوت الدعم بلا `@` | `MyStoreHelpBot` |

بدون `SUPPORT_BOT_TOKEN` يكتفي البوت بتحذير في اللوغ ويعطّل بوت الدعم فقط — الباقي يعمل.

## 5.3 الاشتراك الإجباري في القناة/المجموعة

| المتغير | القيمة | مثال |
|---|---|---|
| `REQUIRED_CHANNEL_ID` | معرّف القناة | `-1001234567890` |
| `REQUIRED_CHANNEL_LINK` | رابطها الكامل | `https://t.me/mystore` |
| `REQUIRED_GROUP_ID` | معرّف المجموعة | `-1009876543210` |
| `REQUIRED_GROUP_LINK` | رابطها الكامل | `https://t.me/mystoregroup` |

> ✅ **البوت يجب أن يكون Admin** في كلٍ منهما، وإلا لن يستطيع التحقق من العضوية.
>
> إن لم ترد اشتراكًا إجباريًا، **اترك هذه الأربعة فارغة أو لا تضفها**. الميزة تُفعَّل تلقائيًا بمجرد وجود قيمة.

## 5.4 قنوات النشر التلقائي

| المتغير | القيمة |
|---|---|
| `UPDATES_CHANNEL_ID` | القناة التي تُنشر فيها المنتجات الجديدة وتنبيهات المخزون |
| `UPDATES_GROUP_ID` | المجموعة، نفس الغرض |

نفس صيغة `-100...`، والبوت Admin فيهما.

## 5.5 الدفع — USDT عبر Binance

| المتغير | القيمة | مثال |
|---|---|---|
| `USDT_TRC20_ADDRESS` | عنوان استقبال TRC20 | `TYt9SJtz3cJhnq5w...` |
| `USDT_BEP20_ADDRESS` | عنوان استقبال BEP20 | `0x0bacd562860a87f8...` |
| `BINANCE_API_KEY` | مفتاح API (قراءة فقط) | `abc123...` |
| `BINANCE_API_SECRET` | السر | `xyz789...` |
| `BINANCE_ID` | رقم Binance Pay الخاص بك | `123456789` |

## 5.6 CryptoBot

| المتغير | القيمة | مثال |
|---|---|---|
| `CRYPTOBOT_TOKEN` | توكن التطبيق من @CryptoBot | `12345:AAxxxx...` |
| `CRYPTOBOT_BOT_URL` | رابط بوت الدفع | `https://t.me/CryptoBot` |

## 5.7 اختيارية — لها قيم افتراضية

| المتغير | الافتراضي | الوصف |
|---|---|---|
| `MIN_DEPOSIT` | `1.00` | أقل مبلغ شحن |
| `REFERRAL_REWARD` | `0.20` | مكافأة الإحالة |
| `CHATGPT_BOT_TOKEN` | — | بوت اشتراكات ChatGPT (اتركه إن لم تحتجه) |
| `CHATGPT_BOT_USERNAME` | — | يوزره بلا `@` |
| `PUBLIC_BASE_URL` | — | رابط مشروعك على Railway |
| `LOG_LEVEL` | `INFO` | `DEBUG` عند تتبع مشكلة |
| `NODE_ENV` | — | `production` |

## 5.8 لا تضفها إطلاقًا

| المتغير | لماذا |
|---|---|
| `WEBHOOK_PORT` / `WEBHOOK_HOST` | لها قيم افتراضية سليمة |
| `RAILWAY_PUBLIC_DOMAIN` | Railway يحقنه تلقائيًا |
| `ETHERSCAN_API_KEY` | غير مستعمل في الكود إطلاقًا (بقايا نسخة قديمة) |
| `JOIN_REQUIRED_ENABLED` | لا يقرأه الكود؛ التفعيل تلقائي عند وجود معرّف قناة/مجموعة |

---

# المرحلة 6 — التشغيل

بعد إضافة المتغيرات، Railway يعيد النشر تلقائيًا. راقب تبويب **Deployments**.

## ما يجب أن تراه في اللوغ

```
[INFO] Telegram bot polling started.
[INFO] 🎫 Support Bot V4 started
[INFO] Webhook server listening on 0.0.0.0:8080
```

## إن ظهر خطأ

| الرسالة | السبب | الحل |
|---|---|---|
| `BOT_TOKEN is required` | التوكن ناقص أو خاطئ | راجع `BOT_TOKEN` |
| `401 Unauthorized` | التوكن غير صحيح | انسخه من BotFather مجددًا |
| `409 Conflict` | نسخة أخرى من البوت تعمل بنفس التوكن | أوقف النسخة القديمة |
| `SQLITE_CANTOPEN` | الـ Volume غير مربوط | راجع المرحلة 4 |

---

# المرحلة 7 — أول إعداد داخل البوت

1. افتح بوتك على تيليجرام وأرسل `/start`
2. أرسل `/admin` — يجب أن تفتح لوحة التحكم
3. من `⚙️ Settings` اضبط:

| الإعداد | التوصية |
|---|---|
| `deposit_max_age_minutes` | `15` — نافذة صلاحية الـ TXID |
| `deposit_intent_ttl_minutes` | `30` — مدة حجز المبلغ |
| `deposit_strict_mode` | `1` — **لا تغيّره**، `0` يفتح ثغرة سرقة الإيداعات |
| `low_stock_threshold_default` | `5` — التنبيه عند انخفاض المخزون |
| `cryptobot_fee_fixed` | `0` إن لم ترد تحميل الزبون رسم CryptoBot |

4. أنشئ تصنيفًا ثم منتجًا، وأضف مخزونًا
5. جرّب شراءً تجريبيًا بحساب آخر

---

# المرحلة 8 — قائمة التحقق النهائية

- [ ] الـ Volume مربوط على `/app/data` و `DB_PATH=/app/data/store.db`
- [ ] `ADMIN_IDS` و `ADMIN_ID` كلاهما فيه رقمك
- [ ] البوت **Admin** في القناة والمجموعة المطلوبتين
- [ ] مفتاح Binance **قراءة فقط**، السحب مطفأ
- [ ] `/admin` يفتح ويعرض: `🔔 Notifications` و `📦 Manual Delivery` و `🛡 Deposit Review`
- [ ] المحفظة → USDT تسأل عن الشبكة ثم المبلغ ثم تعطي رقمًا فريدًا
- [ ] بعد **Redeploy**، المنتج التجريبي ما زال موجودًا
- [ ] لا أخطاء حمراء في اللوغ

---

# نصائح أمنية للمالك الجديد

**1. لا تشارك التوكنات أو مفاتيح API مع أحد.** إن تسرّبت، أعد توليدها فورًا.

**2. مفتاح Binance قراءة فقط.** راجعها الآن إن كنت فعّلت شيئًا آخر بالخطأ.

**3. `deposit_strict_mode` يبقى `1`.** تعطيله يعيد ثغرة تسمح لأي شخص بسرقة إيداعات غيره عبر نسخ الـ TXID من BscScan.

**4. خذ نسخة احتياطية دوريًا.** من Railway → Volume → يمكنك تنزيل محتواه. افعلها أسبوعيًا على الأقل.

**5. راقب `🛡 Deposit Review` بانتظام.** الإيداعات التي لا تطابق حجزًا تنتظر موافقتك هناك.

---

# ملاحظة صغيرة

وصف المشروع في `package.json` يذكر Etherscan — هذا بقايا من نسخة قديمة. التحقق من الإيداعات يتم الآن عبر Binance API. لا يؤثر على شيء، ويمكن تجاهله أو تعديله.
